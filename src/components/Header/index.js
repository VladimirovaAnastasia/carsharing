import React from 'react';
import './styles.scss';

const Header = () => (
    <div className="header">
        <h2 className="header__logo">Need for drive</h2>
        <p className="header__city">Ульяновск</p>
    </div>
);

export default Header;
